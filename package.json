{
	"scripts": {
		"runner:dev": "docker compose up -d --build --force-recreate && docker compose logs -f",
		"runner:scaled": "docker compose up --scale runner=3 -d"
	},
	"author": "Albert Ferguson"
}
