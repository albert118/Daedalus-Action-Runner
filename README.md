# Daedalus GitHub Action Runner

This is a self hosted GitHub Action runner for my server. Nothing fancy.

```sh
# test the runner and show logs
npm run runner:dev
# or run it for real at scale
npm run runner:scaled
```
